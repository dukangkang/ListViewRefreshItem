<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <ImageView
        android:id="@+id/listview_item_iv"
        android:layout_width="120dp"
        android:layout_height="100dp"
        android:src="@drawable/ic_launcher"/>

    <TextView
        android:id="@+id/listview_item_title"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_toRightOf="@+id/listview_item_iv"
        android:text="Title"
        android:textColor="@drawable/comm_txt_color"/>

    <TextView
        android:id="@+id/listview_item_desc"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@+id/listview_item_title"
        android:layout_marginTop="14dp"
        android:layout_toRightOf="@+id/listview_item_iv"
        android:text="Desc"
        android:textColor="@drawable/comm_txt_color"/>

</RelativeLayout>
